# LLMoker QA Result 2026-03-26

이 문서는 [qa_20260326.md](/home/hosung/pytorch-demo/LLMoker/docs/qa_20260326.md) 기준으로 2026-03-26에 진행한 검수와 재검증 결과를 정리한다.

기준:

- 코드 검수: 엔진, UI 흐름, LLM 회고 경로
- 자동 검증: Ren'Py compile/lint, Python `py_compile`
- 실제 런타임 검증: `verify_llm_policy_simulation.py`로 실제 포커 엔진 + 실제 Qwen 런타임 사용
- 영상 검토: 2026-03-25 23:56:23 플레이 영상 contact sheet 확인


## 이번 QA에서 실제로 확인한 것

### 1. 게임 흐름 / UI

- `마지막 행동 -> 결과 화면` 순서
  - 상태: `부분 해결`
  - 현재 코드는 [poker_minigame.rpy](/home/hosung/pytorch-demo/LLMoker/llmoker/game/poker_minigame.rpy)에서 마지막 베팅 행동 후 `play_dialogue_event("betting", round_messages)`를 먼저 호출한 뒤 `round_end`로 넘어간다.
  - 즉 구조상 `행동 나레이션 -> 결과 화면` 순서는 맞다.
  - 다만 전환 템포가 짧아 실제 체감상 급하게 느껴질 가능성은 남아 있다.

- 체크 종료 전달
  - 상태: `미해결`
  - 엔진상으론 `플레이어 체크 -> 사야 체크`로 베팅 라운드가 닫히는 것이 맞다.
  - 하지만 화면 나레이션은 `두 사람 모두 체크해서 종료`를 직접 말하지 않아, 여전히 “체크 한 번으로 끝난 것처럼” 느껴질 수 있다.

- 폴드 종료 전달
  - 상태: `부분 해결`
  - [poker_dialogue.rpy](/home/hosung/pytorch-demo/LLMoker/llmoker/game/poker_dialogue.rpy)에서 `플레이어가 폴드했고...`, `사야가 폴드했고...` 문구는 들어갔다.
  - 다만 쇼다운 종료와 비교해 폴드 종료의 연출 차이는 아직 약하다.

- 도움말 규칙 설명
  - 상태: `부분 해결`
  - 도움말은 현재 규칙 설명을 담고 있다.
  - 다만 정보 밀도가 높아 읽기 피곤한 문제는 남아 있다.


### 2. 엔진 메커니즘

- `to_call == 0`인데 폴드가 가능한 문제
  - 상태: `해결`
  - 현재 엔진은 무압박 상태에서 `check, bet`만 허용한다.
  - `fold`는 실제로 상대 베팅/레이즈가 있을 때만 열린다.

- 레이즈 후 바로 쇼다운으로 건너뛰는지
  - 상태: `비재현`
  - 현재 엔진 로직상 `raise`는 바로 종료가 아니라 상대 응답 차례로 넘긴다.
  - 즉 “레이즈 직후 응답 없이 쇼다운”은 현재 코드상 정상 흐름이 아니다.


### 3. LLM 행동 / 드로우 / 회고

- 회고가 행동을 오염시키는 문제
  - 상태: `미해결`
  - 코드 검수와 실제 시뮬레이션 모두에서 재현됐다.
  - 한 번 저장된 회고가 다음 라운드 행동/드로우 프롬프트에 그대로 들어가며, 약한 상황의 일반론이 강한 패까지 번지는 위험이 여전히 크다.

- 드로우가 족보를 부수는 문제
  - 상태: `부분 해결`
  - 엔진에 `완성 족보 보호 인덱스` 하드 제약을 넣어, 원페어/투페어/트리플/스트레이트 이상을 이루는 카드는 실제 교체 직전에 차단한다.
  - 실제 Qwen 시뮬레이션에서도 스트레이트 손패에서 `요청 인덱스 [0, 2]`가 `차단 인덱스 [0, 2]`로 교정되는 것을 확인했다.
  - 다만 드로우 이유 문장 자체는 여전히 틀릴 수 있고, 하이카드 상태에서 어떤 카드를 버릴지는 여전히 불안정하다.

- 행동 이유가 현재 상태와 맞지 않는 문제
  - 상태: `미해결`
  - 최근 전략 문장을 가져다 붙이는 방식이 강해서, 현재 액션과 정확히 연결되지 않는 이유 문장이 여전히 나온다.


## 직접 검증 결과

### A. 자동 검증

실행:

```bash
cd /home/hosung/pytorch-demo/LLMoker/llmoker
./scripts/check_game_non_gui.sh
```

결과:

- `Ren'Py compile` 통과
- `Ren'Py lint` 통과
- `Python compile` 통과


### B. 실제 Qwen 정책 회고 시뮬레이션

실행:

```bash
cd /home/hosung/pytorch-demo/LLMoker/llmoker
rm -f /tmp/llmoker_policy_verify_memory.sqlite3 /tmp/llmoker_policy_verify_replays.sqlite3
./.venv/bin/python ./scripts/verify_llm_policy_simulation.py
```

결과:

- 상태: `실패`
- 현재 direct-written policy 구조에서 Qwen이 `short_term / long_term / strategy_focus`를 직접 작성하도록 바꾼 뒤, 역할 용어 검증에서 실패했다.
- 실제 실패 예:
  - `정책 회고 long_term에 self 주체가 드러나야 합니다.`

의미:

- 지금 구조는 “LLM이 회고 문장을 직접 쓰게 한다”는 방향으로는 바뀌었지만,
- 실제 Qwen 4B가 이 형식과 역할 용어 규칙을 안정적으로 지키는지는 아직 검증 통과 수준이 아니다.


### C. 드로우 보호 인덱스 단위 검증

실행:

```bash
cd /home/hosung/pytorch-demo/LLMoker/llmoker
python3 - <<'PY'
from backend.poker_engine import PokerMatch
m = object.__new__(PokerMatch)
print('pair', PokerMatch._protected_hand_indexes(m, [('Ace','Hearts'),('Ace','Clubs'),('5','Spades'),('9','Diamonds'),('King','Hearts')]))
print('two_pair', PokerMatch._protected_hand_indexes(m, [('Ace','Hearts'),('Ace','Clubs'),('5','Spades'),('5','Diamonds'),('King','Hearts')]))
print('trips', PokerMatch._protected_hand_indexes(m, [('Ace','Hearts'),('Ace','Clubs'),('Ace','Spades'),('5','Diamonds'),('King','Hearts')]))
print('straight', PokerMatch._protected_hand_indexes(m, [('10','Hearts'),('Jack','Clubs'),('Queen','Spades'),('King','Diamonds'),('Ace','Hearts')]))
PY
```

결과:

- `pair -> [0, 1]`
- `two_pair -> [0, 1, 2, 3]`
- `trips -> [0, 1, 2]`
- `straight -> [0, 1, 2, 3, 4]`

의미:

- 현재 엔진은 완성 족보를 이루는 카드 인덱스를 올바르게 보호 인덱스로 계산한다.
- LLM이나 스크립트 봇이 이 인덱스를 버리려고 해도 실제 교체 직전 엔진에서 차단된다.


## 멀티 에이전트 검수 요약

- LLM/정책 검수
  - 회고 메모리가 행동/드로우 프롬프트에 무가공으로 다시 들어가고 있어 오염 위험이 크다고 확인
  - 플레이어/사야 역할 지칭 혼선이 현재 오류의 핵심 원인 중 하나라고 확인

- 게임 흐름/UI 검수
  - 마지막 행동 나레이션 후 결과 화면 순서는 코드상 맞지만
  - 체크 종료 설명과 폴드 종료 연출은 여전히 사용자 체감상 약하다고 확인


## 오늘 결론

### 해결된 것

- 무압박 상태에서 폴드가 열리던 엔진 문제
- 마지막 행동 뒤 곧바로 결과 화면으로 점프하던 흐름 일부
- 폴드 종료 문구가 화면에서 완전히 사라지던 문제
- 드로우에서 완성 족보 구성 카드를 그대로 버리던 문제의 핵심 경로

### 아직 해결 안 된 것

- 회고가 다음 행동을 과도하게 오염시키는 문제
- 드로우 이유와 실제 교체 의도가 자주 어긋나는 문제
- 행동 이유와 실제 상황의 불일치
- 체크 종료 / 폴드 종료의 전달력 부족
- direct-written policy를 Qwen 4B가 안정적으로 지키지 못하는 문제


## 다음 작업 권장 순서

1. 행동 이유와 회고에서 `self/opponent` 역할 용어를 끝까지 일관되게 유지하도록 프롬프트와 후처리를 더 다듬는다.
2. 드로우 이유 문장과 실제 교체 결과가 어긋나지 않도록 드로우 프롬프트를 더 좁힌다.
3. 정책 회고 direct-written 구조는 유지하되, 현재 Qwen 4B가 실제로 통과 가능한 수준까지 검증 규칙과 프롬프트를 다시 맞춘다.
