"""이 폴더는 예전 vLLM 서버-클라이언트 구조 백업본을 보관한다."""

현재 게임이 실제로 쓰는 경로는 `backend/llm/client.py`, `backend/llm/runtime.py`의
transformers 런타임이다.

이 폴더는 아래 이유로만 남겨 둔다.

- 예전 `vLLM` 서버-클라이언트 구조를 공부할 수 있게 하기
- 필요하면 이후 실험용 브랜치에서 다시 참고하기

여기 파일은 현재 실행 경로가 아니다.
